{
    "name": "lon-lat-parser",
    "description": "tries to recognize lat / lon in a string and returns them",
    "version": "0.1.0",
    "main": "lon-lat-parser.js",
    "dependencies": {
        "lodash": "^4.17.10"
    },
    "devDependencies": {
        "mocha": "^2.1.0",
        "should": "^4.4.2"
    },
    "engines": {
        "node": "0.10.x",
        "npm": "^1.3.0"
    },
    "scripts": {
        "test": "mocha test"
    }
}
